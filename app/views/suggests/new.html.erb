<div class="container mx-auto p-4 md:p-8">
  <div class="card bg-white shadow-lg rounded-xl px-8 pt-6 pb-8 mb-8 relative max-w-3xl mx-auto">
    
    <h2 class="text-3xl font-semibold text-primary text-center flex items-center justify-center mb-6">
      <%= image_tag 'ai_suggests.png', alt: "AI", class: "w-14 h-14 mr-4" %>
      AIに相談する
    </h2>
    
    <p class="text-center text-gray-600 mb-6 text-lg">
      if-thenルールについて、アドバイスがありますか？<br>
      もっと良い提案をお伝えします！！
    </p>
    
    <%= form_with(model: @suggest, local: true, html: { id: "suggest-form" }) do |f| %>
      <div class="form-control mb-6">
        <%= f.label :input, "相談内容", class: "label text-lg font-medium text-gray-700" %>
        <%= f.text_area :input, class: "textarea textarea-bordered h-32 resize-none bg-white shadow-lg focus:ring-2 focus:ring-accent", 
                        placeholder: "例:学習机に座ったら、勉強を5分始める", 
                        required: true %>
      </div>
      
      <div class="form-control mb-6 flex justify-center">
        <%= f.submit "提案を受ける！", class: "btn btn-primary text-base-100 w-full md:w-auto shadow-lg hover:shadow-xl transition-all py-3 px-8 rounded-xl" %>
      </div>
    <% end %>
    
    <div class="text-center mt-6">
      <%= link_to "提案一覧へ", suggests_path, class: "text-accent underline font-semibold" %>
    </div>
  </div>
</div>
