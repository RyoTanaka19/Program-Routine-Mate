<!-- 全体を包むコンテナ。レスポンシブ対応で左右に余白を持たせる -->
<div class="container mx-auto p-4 sm:p-6 md:p-8">
  
  <!-- カード形式のコンテンツを作成。背景白、影、丸みを付けて、最大幅を設定 -->
  <div class="card bg-white shadow-lg rounded-xl px-4 sm:px-6 md:px-8 pt-6 pb-8 mb-8 relative max-w-full sm:max-w-2xl md:max-w-3xl mx-auto">
    
<!-- 見出しを作成。AIのアイコンと「AIに相談する」のテキストを中央に配置 -->
<h2 class="text-2xl sm:text-3xl font-semibold text-center flex items-center justify-center mb-6">
  <%# アイコン画像を表示。AIに関連するセクションであることを示す %>
  <%= image_tag 'ai_suggests.png', alt: "AI", class: "w-12 h-12 sm:w-14 sm:h-14 mr-4" %>
  <span class="text-green-500">AIコーチに相談する</span>
</h2>

    
    <!-- 説明文を表示。AIに相談する前に簡単な案内文を表示 -->
    <p class="text-center text-gray-600 mb-6 text-base sm:text-lg">
      if-thenルールについて、アドバイスがありますか？<br>
      もっと良い提案をお伝えします！！
    </p>
    
    <!-- フォームを作成。@suggestモデルを使って提案を送信 -->
    <%= form_with(model: @suggest, local: true, html: { id: "suggest-form" }) do |f| %>
      
      <!-- 相談内容を入力するためのテキストエリア -->
      <div class="form-control mb-6">
        <%# 「相談内容」のラベルとテキストエリアを表示 %>
        <%= f.label :input, "相談内容", class: "label text-lg font-medium text-gray-700" %>
        <%= f.text_area :input, class: "textarea textarea-bordered h-32 resize-none bg-white shadow-lg focus:ring-2 focus:ring-accent w-full", 
                        placeholder: "例:学習机に座ったら、勉強を5分始める", 
                        required: true %>
      </div>
      
<!-- 提案を受けるための送信ボタン -->
<div class="form-control mb-6 flex justify-center">
  <%= f.submit "提案を受ける！",
    class: "bg-green-500 hover:bg-green-600 text-white text-base w-full sm:w-auto shadow-lg hover:shadow-xl transition-all py-3 px-6 sm:px-8 rounded-xl",
    data: { action: "click->loading#show" } %>
</div>

    <% end %>
    
    <!-- 提案一覧ページへのリンク -->
    <div class="text-center mt-6">
      <%= link_to "提案一覧へ", suggests_path, class: "text-accent underline font-semibold" %>
    </div>
  </div>
</div>
