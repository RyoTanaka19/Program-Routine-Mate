<%= turbo_frame_tag "like-count-#{study_log.id}" do %>
  <div class="flex items-center gap-2 text-gray-700 text-lg mt-2">
    <!-- ❤️ハートアイコン（統一感を意識） -->
    <i class="fas fa-heart text-red-400"></i>
    <span><%= study_log.study_likes.count %></span>
  </div>
<% end %>
