<%= link_to '学習時間の設定', new_studying_session_path, class: 'px-8 py-4 bg-green-600 text-white font-semibold rounded-xl hover:bg-green-700 transition duration-300 mb-8 inline-block shadow-lg text-center w-full max-w-xs mx-auto' %>


<h1 class="text-3xl font-bold mb-8 text-center">学習セッションカレンダー</h1>
<%= month_calendar(events: @studying_sessions, class: "grid grid-cols-7 gap-2") do |date, sessions| %>
  <div class="border rounded-lg p-4 min-h-[120px] bg-white shadow-sm flex flex-col text-left">
    <div class="text-sm font-semibold text-blue-700 mb-2">
      <%= date.day %>
    </div>
    <% if sessions.any? %>
      <ul class="text-sm text-gray-700 space-y-1">
        <% sessions.each do |session| %>
          <li class="flex items-center gap-1">
            <span>🕒</span>
            <span><%= session.start_time.strftime("%H:%M") %>〜<%= session.end_time.strftime("%H:%M") %></span>
          </li>
        <% end %>
      </ul>
    <% else %>
      <span class="text-xs text-gray-400">予定なし</span>
    <% end %>
  </div>
<% end %>
